info:
  name: Clients List
  type: http
  seq: 1

http:
  method: GET
  url: "{{clientBaseUrl}}/client"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Product List
  type: http
  seq: 1

http:
  method: GET
  url: "{{productBaseUrl}}/product"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

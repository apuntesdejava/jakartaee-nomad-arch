info:
  name: Health Check
  type: http
  seq: 3

http:
  method: GET
  url: "{{SALES_HOST}}/health/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
